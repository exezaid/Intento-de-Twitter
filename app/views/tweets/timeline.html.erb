<h1>Timeline</h1>

<table>
<% @all_tweets.each do |tweet| %>
  <tr>
    <td><%= link_to tweet.user.name, tweets_path(:user_id => tweet.user_id) %></td>
    <td><%= tweet.body %></td>
    <td><%= tweet.created_at %></td>
  </tr>
<% end %>
</table>

<br />

